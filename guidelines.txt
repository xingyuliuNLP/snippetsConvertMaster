# function 1 : record existence of symbols in a line and return a list

check dollar position (beginning/end) | done
check brackets chooseDirectory$ ("$1:ChooseDirectory") | done
check "$1:type $2:name" | > "${1:type} ${2:name}" | done
check ChooseDirectory$:  if end with $:, set every mark to zero (do nothing) | done
check space (if startwith " " then "  "+string)
if $1
  $2
def:
 sfsdfsd
 return dictionary [dollar:1,colon:0,hash:1,x:0]
  # function 2 : remove everything
 # function 3 : make modifications according to the dictionary returned in function 1
 if dollar = 1 : etc...





 to be optimized:
let users name their input files

for sublime, atom, vim

